package org.IIITD.Question_1;

public interface RotateAndFly extends Rotatable {
    public void fly();
    public void rotate(); // doesn't need to mention - it already inherits from rotatable
}
